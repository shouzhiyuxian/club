# 高校社团成员管理系统功能清单与系统介绍

## 1. 项目概述

这是一个基于 Django 的高校社团成员管理系统，功能覆盖社团管理、成员管理、活动管理、活动报名、招新管理、公告管理、社交互动与个人主页。系统采用后端渲染模式，前端使用 Bootstrap、MDB5、jQuery 等技术实现页面交互与样式。

系统目标是为高校社团提供一个可管理社团信息、成员信息、活动信息、招新流程与内部互动的完整平台，适合用于毕业设计的功能说明与扩写基础。

---

## 2. 技术栈与项目结构

- 后端框架：Django 4.1
- 数据库：MySQL（项目中也包含 SQLite 配置）
- 前端：Bootstrap 5.3、MDB5、jQuery、Font Awesome、Bootstrap Icons
- 文件上传：成员头像、活动照片
- 导出功能：Excel 导出（`openpyxl` 或自定义导出工具）

项目目录结构：

- `manage.py`：Django 启动入口
- `Learntest/`：Django 项目配置
- `app01/`：主应用
  - `models.py`：数据模型定义
  - `views.py`：首页视图
  - `srcs/forms/form.py`：表单定义与验证
  - `srcs/views/`：功能模块视图
  - `srcs/utils/`：导出、角色权限、分页等工具
  - `middle_ware/my_auth.py`：认证中间件
  - `static/`：静态资源
  - `templates/`：页面模板

---

## 3. 用户角色与权限

系统支持三种登录身份：

1. 管理员（admin）
   - 登录方式：账号 + 密码
   - 权限：可访问并管理全部功能模块
2. 社长（president）
   - 登录方式：学号 + 密码
   - 权限：仅管理所属社团的社团信息、成员、活动、报名、招新、公告等
   - 社长视角：可创建本社团招新、审核招新报名、移交社长、发布本社团公告
3. 普通成员（member）
   - 登录方式：学号 + 密码
   - 权限：查看本人信息、可报名本社团的活动、查看个人主页、参与社团讨论、关注其他成员

系统通过 `session` 保存登录状态，并在 `app01/srcs/utils/role_helper.py` 中封装角色判断逻辑。

---

## 4. 功能模块清单

### 4.1 登录与认证

- 统一登录入口：`/login/`
- 管理员账号登录：`MyAdmin.account + password`
- 成员账号登录：`Member.member_id + password`
- 密码使用 MD5 加密存储
- 注销功能：`/logout/`

### 4.2 管理员管理

- 管理员列表：`/myadmin/list`
- 添加管理员：`/myadmin/add`
- 编辑管理员：`/myadmin/<nid>/edit/`
- 删除管理员：`/myadmin/delete/`
- 管理员密码重置：`/myadmin/<nid>/reset/`

管理员信息包括登录账号、管理员姓名、密码。

### 4.3 社团管理

- 社团列表：`/club/list`
- 添加社团：`/club/add`
- 编辑社团：`/club/<int:nid>/edit/`
- 删除社团：`/club/delete/`
- 转让社长：`/club/transfer/`

社团信息包含：名称、简介、成立日期、社长姓名、联系方式、邮箱、创建时间。社长可创建本社团，管理员可管理所有社团。

### 4.4 角色管理

- 角色列表：`/role/list`
- 添加角色：`/role/add`
- 编辑角色：`/role/<int:nid>/edit/`
- 删除角色：`/role/delete/`

角色定义用于成员身份管理，系统内至少包含：`社长`、`普通成员`。`Role.level` 级别区分权限。

### 4.5 成员管理

- 成员列表：`/member/list`
- 添加成员：`/member/add`
- 编辑成员：`/member/<nid>/edit/`
- 删除成员：`/member/delete/`
- 管理员重置成员密码：`/member/<nid>/reset/`

成员信息字段：学号、姓名、昵称、头像、性别、年级、专业、手机号、邮箱、所属社团、所属部门、角色、加入时间、登录密码。

成员管理逻辑包括：

- 管理员查看所有成员；社长/成员仅查看本社团成员
- 社长添加成员时自动限制到本社团
- 删除成员时会清理相关招新报名记录
- 编辑成员时支持头像上传、信息修改、社团/角色同步

### 4.6 活动管理

- 活动列表：`/activity/list`
- 添加活动：`/activity/add`
- 编辑活动：`/activity/<int:nid>/edit/`
- 查看活动详情：`/activity/<int:nid>/detail/`
- 批量标记已参加：`/activity/<int:nid>/mark-attended/`
- 删除活动：`/activity/delete/`
- AJAX 获取社团成员：`/activity/get-members/`

活动信息字段：活动名称、社团、活动描述、地点、开始时间、结束时间、人数上限、当前参与人数、组织者、状态、创建时间。

关键行为：

- 社长仅管理本社团活动
- 添加/编辑活动时，可选择组织者
- 组织者会自动报名并标记为已参加
- 活动详情显示报名列表、评论、照片、点赞统计
- 活动状态支持：报名中、进行中、已结束、已取消

### 4.7 活动报名管理

- 报名列表：`/registration/list`
- 添加报名：`/registration/add`
- 编辑报名：`/registration/<int:nid>/edit/`
- 删除报名：`/registration/delete/`

报名信息字段：活动、成员、状态。

功能特性：

- 自动防止重复报名（`unique_together = [['activity', 'member']]`）
- 管理员可管理全部报名，社长仅管理本社团活动报名
- 报名/删除操作会自动更新活动当前参与人数
- 报名状态包括：已报名、已参加、已取消、未参加

### 4.8 招新管理

#### 招新批次

- 招新批次列表：`/recruitment/list`
- 创建批次：`/recruitment/add`
- 编辑批次：`/recruitment/<int:nid>/edit/`
- 删除批次：`/recruitment/delete/`

批次信息字段：社团、批次名称、开始日期、结束日期、状态（报名中 / 已结束）、创建时间。

#### 招新报名

- 招新申请列表：`/recruitment/applications/`
- 提交报名：`/recruitment/applications/add/`
- 编辑申请：`/recruitment/applications/<int:nid>/edit/`
- 删除申请：`/recruitment/applications/delete/`
- 审核通过：`/recruitment/applications/<int:nid>/approve/`
- 审核拒绝：`/recruitment/applications/<int:nid>/reject/`
- 成员报名入口：`/recruitment/<int:nid>/apply/`

报名信息字段：招新批次、学号、姓名、性别、年级、专业、手机号、邮箱、申请部门、状态（待审核/已通过/已拒绝）、申请时间、备注。

成员端招新逻辑：

- 成员只能报名本社团当前状态为“报名中”的招新批次
- 申请时会自动带出个人信息
- 社长可审核报名并在通过后更新成员社团身份
- 报名状态与成员退出社团时的状态维护关联

### 4.9 公告管理

- 公告列表：`/announcement/list`
- 添加公告：`/announcement/add`
- 编辑公告：`/announcement/<int:nid>/edit/`
- 删除公告：`/announcement/<int:nid>/delete/`
- 公告详情：`/announcement/<int:nid>/detail/`

公告字段：标题、内容、所属社团、发布者、发布者姓名、发布时间、状态（发布/草稿）、置顶。

权限规则：

- 管理员可发布全局公告并管理所有公告
- 社长可发布本社团公告并管理本社团公告
- 普通成员只能查看已发布的全局公告和本社团公告

### 4.10 成员个人视角

成员端页面与功能：

- 个人信息页面：`/member/profile/`
- 修改密码：`/member/change-password/`
- 退出社团：`/member/leave-club/`
- 我的报名：`/member/my-registrations/`
- 可报名活动：`/member/activities/`
- 活动详情（成员端）：`/member/activity/<int:activity_id>/`
- 社团讨论：`/member/discussion/`
- 删除讨论发言：`/member/discussion/<int:discussion_id>/delete/`

成员端特点：

- 个人资料可编辑部分字段，学号/社团/角色只读
- 自助报名本社团报名中活动
- 查看本人报名历史与活动参与记录
- 退出社团时自动清理社团关联与招新状态

### 4.11 个人主页与关注

- 个人主页：`/profile/<str:member_id>/`
- 关注/取消关注：`/profile/<str:member_id>/follow/`

个人主页内容：

- 个人资料、加入社团、参与活动
- 关注数、粉丝数
- 共同活动和好友关系
- 权限控制：管理员可查看所有；成员仅能查看本社团成员或自己

关注功能：

- 防止自己关注自己
- 仅允许可查看的成员之间关注或取消关注
- 关注关系写入 `Follow` 数据表

### 4.12 社团讨论与互动

系统提供内部社团讨论与活动互动功能：

- 社团讨论发布与删除（成员、自身或社长删除）
- 活动评论、点赞、照片上传
- 成员端活动详情支持评论、点赞、上传照片
- 活动详情统计评论数、照片数、点赞数

交互模型包含：

- `ActivityComment`：活动评论
- `ActivityPhoto`：活动照片
- `ActivityLike`：活动点赞
- `ClubDiscussion`：社团讨论

### 4.13 数据导出与报表

系统支持导出 Excel：

- 社团列表导出
- 成员列表导出
- 活动列表导出
- 活动报名列表导出
- 招新批次导出
- 招新报名列表导出

导出实现基于自定义工具 `app01/srcs/utils/export_excel.py`，将数据转换为 Excel 文件并下载。

---

## 5. 数据模型概览

### 核心实体与关系

- `MyAdmin`：管理员账号
- `Club`：社团
- `Department`：社团内部部门，关联 `Club`
- `Role`：成员角色，包含 `社长` 与 `普通成员`
- `Member`：成员，属于社团、部门、角色
- `Activity`：社团活动，关联社团和组织者成员
- `ActivityRegistration`：活动报名，关联活动与成员
- `Recruitment`：招新批次，关联社团
- `RecruitmentApplication`：招新报名，关联批次、部门、成员信息
- `Announcement`：公告，可绑定社团或全局
- `Follow`：关注关系，成员之间互相关注
- `ActivityComment`：活动评论
- `ActivityPhoto`：活动照片
- `ActivityLike`：活动点赞
- `ClubDiscussion`：社团讨论

### 关键字段说明

- `Member.member_id`：学号，作为登录账号和主键
- `Member.password`：MD5 加密密码
- `Club.president`：社长姓名
- `Role.level`：1=社长，2=普通成员
- `Activity.status`：活动状态
- `ActivityRegistration.status`：报名状态
- `Recruitment.status`：招新状态
- `RecruitmentApplication.status`：申请状态
- `Announcement.is_top`：置顶标记
- `Follow.unique_together`：防止重复关注
- `ActivityRegistration.unique_together`：防止重复活动报名
- `ActivityLike.unique_together`：防止重复点赞

---

## 6. 关键实现与业务逻辑

### 权限与视图控制

- `get_request_role(req)` 统一判断当前用户身份：`admin` / `president` / `member`
- 管理员拥有全部权限
- 社长权限限定到所属社团
- 普通成员只能访问个人页面、报名页面、社团讨论

### 社长与社团关系同步

- 社团新增或编辑时，`ClubModelForm` 会选择社长成员
- `MemberModelForm.save()` 会同步 `Club.president`
- 社长身份变更时，系统自动将原社长降级为普通成员
- 转让社长功能 `/club/transfer/` 保证社长身份安全切换

### 活动报名与人数统计

- 活动报名新增、删除、编辑都会更新 `Activity.current_participants`
- 组织者自动报名为已参加
- 成员自助报名时只能报名本社团的“报名中”活动
- 批量“标记已参加”功能用于活动后审核

### 招新审核流程

- 招新批次由管理员或社长创建
- 招新报名可由成员提交，或由社长后台添加
- 审核通过会同步成员社团归属与角色
- 退出社团时会更新成员已通过招新申请状态，避免重复冲突

### 成员个人主页与关注逻辑

- 展示成员个人资料、参与活动、关注数、粉丝数
- 仅同社团成员可互相查看主页（管理员除外）
- 关注操作使用 POST 请求，返回 JSON

### 社交与互动

- 活动详情允许评论、点赞、照片上传
- 社团讨论区用于内部交流
- 成员关注构建社交网络

---

## 7. 路由与页面分布

### 主要 URL 列表

- `/`：系统首页（仪表盘）
- `/login/`：登录页
- `/logout/`：退出登录
- `/myadmin/*`：管理员管理
- `/club/*`：社团管理
- `/role/*`：角色管理
- `/member/*`：成员管理
- `/activity/*`：活动管理
- `/registration/*`：活动报名管理
- `/recruitment/*`：招新批次与报名
- `/announcement/*`：公告管理
- `/member/profile/`：成员个人信息
- `/member/change-password/`：成员密码修改
- `/member/leave-club/`：成员退社
- `/member/discussion/`：社团讨论
- `/member/my-registrations/`：我的报名
- `/member/activities/`：可报名活动
- `/member/activity/<activity_id>/`：成员活动详情
- `/profile/<member_id>/`：用户个人主页
- `/profile/<member_id>/follow/`：关注/取消关注

---

## 8. 运行与部署说明

### 1. 安装依赖

```bash
pip install -r requirement.txt
```

### 2. 配置数据库

`Learntest/settings.py` 中配置 MySQL:

```python
DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.mysql',
        'NAME': 'club',
        'USER': 'root',
        'PASSWORD': 'root',
        'HOST': '127.0.0.1',
        'PORT': '3306',
    }
}
```

也可使用 SQLite：

```python
DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.sqlite3',
        'NAME': BASE_DIR / 'db.sqlite3',
    }
}
```

### 3. 初始化数据库

```bash
python manage.py makemigrations
python manage.py migrate
```

### 4. 创建管理员账户

```python
from app01.utils.md5 import get_md5
from app01.models import MyAdmin
MyAdmin.objects.create(
    id='admin',
    user_name='admin',
    password=get_md5('12345678')
)
```

### 5. 启动应用

```bash
python manage.py runserver
```

访问地址：`http://127.0.0.1:8000/`

---

## 9. 未来扩展建议

- 增加社团审核与社团状态管理
- 增加通知消息推送功能
- 增加统计图表与仪表盘信息
- 增加活动报名人数上限提醒与签到功能
- 增加社团公告分类与用户消息中心
- 增加前后端分离、REST API 与移动端支持

---

## 10. 备注

本文档基于当前系统代码和路由设计生成，适合作为毕业设计的功能说明与扩写基础。后续扩写可直接基于上述模块、数据模型、权限设计与业务逻辑进行深化。