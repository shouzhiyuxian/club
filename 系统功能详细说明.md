# 高校社团成员管理系统详细说明

## 1. 系统概述

该系统是一个基于 Django 的高校社团管理平台，旨在实现社团组织结构、成员管理、活动管理、招新管理与社交互动的完整业务流程。系统支持三类用户角色：管理员、社长、普通成员。系统通过 Django 模板、Bootstrap、jQuery 实现前后端配合，采用 MySQL 或 SQLite 存储数据。

系统设计重点：
- 完整的社团业务模型与人员关系管理
- 角色权限控制与视图级筛选
- 活动与报名双向联动
- 招新报名审核流程
- 成员个人主页与互动功能
- Excel 导出与分页支持

## 2. 系统架构与关键目录

- `manage.py`：Django 项目入口
- `Learntest/settings.py`：项目配置，数据库、静态文件、媒体目录等
- `Learntest/urls.py`：全局 URL 路由入口
- `app01/models.py`：数据库实体模型定义
- `app01/srcs/views/`：业务视图逻辑，实现各功能页面请求处理
- `app01/srcs/forms/form.py`：表单模型与验证逻辑
- `app01/srcs/utils/`：常用工具（分页、Excel 导出、角色判断、MD5 加密）
- `templates/`：页面模板文件
- `static/`：静态资源文件

## 3. 业务角色与权限

### 3.1 管理员（admin）

- 登录凭证：`MyAdmin.account + password`
- 会话类型：`req.session['info']['type'] = 'admin'`
- 权限范围：查看、添加、编辑、删除所有社团、部门、角色、成员、活动、报名、招新、公告、管理员账号
- 仪表盘：全平台数据汇总及按社团切换查看

### 3.2 社长（president）

- 登录凭证：`Member.member_id + password`
- 会话类型：`req.session['info']['type'] = 'president'`
- 权限范围：管理本社团范围内的社团、部门、成员、活动、活动报名、招新、公告；查看本社团数据仪表盘
- 限制：不能管理跨社团数据、不能访问管理员账户管理

### 3.3 普通成员（member）

- 登录凭证：`Member.member_id + password`
- 会话类型：`req.session['info']['type'] = 'member'`
- 权限范围：查看与编辑本人资料、修改密码、报名活动、查看可报名活动、查看个人主页、参与社团讨论、关注其他成员
- 限制：不能管理社团/部门/角色/活动/招新/公告等后台管理功能

### 3.4 权限判断函数

- `app01.srcs.utils.role_helper.get_request_role(req)`：返回 `(role_type, club_id, member_id)`
- `admin_required`：仅管理员可访问
- `president_or_admin_required`：仅管理员或社长可访问

## 4. 数据模型详细说明

### 4.1 `MyAdmin`

- `id`：主键，字符串
- `account`：登录账号，唯一
- `user_name`：管理员姓名
- `password`：MD5 加密后的密码

### 4.2 `Club`

- `club_id`：自增主键
- `name`：社团名称
- `description`：社团简介
- `established_date`：成立日期
- `president`：社长姓名
- `contact_phone`：联系电话
- `contact_email`：联系邮箱
- `create_time`：创建时间

### 4.3 `Department`

- `department_id`：自增主键
- `name`：部门名称
- `club`：关联社团
- `description`：部门简介
- `create_time`：创建时间

### 4.4 `Role`

- `role_id`：自增主键
- `name`：角色名称
- `level`：角色等级
  - `1`：社长
  - `2`：普通成员

### 4.5 `Member`

- `member_id`：学号，主键
- `name`：姓名
- `nickname`：昵称
- `avatar`：头像文件
- `gender`：性别（1 男 / 2 女）
- `grade`：年级（1-大一 / 2-大二 / 3-大三 / 4-大四 / 5-研究生）
- `major`：专业
- `phone`：手机号
- `email`：邮箱
- `club`：所属社团
- `department`：所属部门
- `role`：角色关联
- `join_time`：加入时间
- `password`：登录密码（MD5 存储）

### 4.6 `Activity`

- `activity_id`：自增主键
- `title`：活动名称
- `club`：所属社团
- `description`：活动描述
- `location`：活动地点
- `start_time`：开始时间
- `end_time`：结束时间
- `max_participants`：最大参与人数
- `current_participants`：当前参与人数
- `organizer`：组织者成员
- `status`：状态（1 报名中 / 2 进行中 / 3 已结束 / 4 已取消）
- `create_time`：创建时间

### 4.7 `ActivityRegistration`

- `registration_id`：自增主键
- `activity`：关联活动
- `member`：关联成员
- `status`：报名状态（1 已报名 / 2 已参加 / 3 已取消 / 4 未参加）
- 约束：`unique_together = [['activity', 'member']]`，避免重复报名

### 4.8 `Recruitment`

- `recruitment_id`：自增主键
- `club`：所属社团
- `title`：批次名称
- `start_date`：开始日期
- `end_date`：结束日期
- `status`：状态（1 报名中 / 2 已结束）
- `create_time`：创建时间

### 4.9 `RecruitmentApplication`

- `application_id`：自增主键
- `recruitment`：关联招新批次
- `student_id`：申请学号
- `name`：申请姓名
- `gender`：性别
- `grade`：年级
- `major`：专业
- `phone`：手机号
- `email`：邮箱
- `apply_department`：申请部门
- `status`：状态（1 待审核 / 2 已通过 / 3 已拒绝）
- `apply_time`：申请时间
- `remark`：备注
- 约束：`unique_together = [['recruitment', 'student_id']]`

### 4.10 `Announcement`

- `announcement_id`：自增主键
- `title`：公告标题
- `content`：公告内容
- `club`：所属社团（可为全局公告）
- `publisher`：发布管理员
- `publisher_name`：发布者姓名
- `publish_time`：发布时间
- `status`：状态（1 发布 / 2 草稿）
- `is_top`：是否置顶
- 排序：`ordering = ['-is_top', '-publish_time']`

### 4.11 社交互动相关模型

- `Follow`：成员关注，约束 `unique_together = [['follower', 'followed']]`
- `ActivityComment`：活动评论，按 `create_time` 正序排列
- `ActivityPhoto`：活动照片，按 `upload_time` 降序排列
- `ActivityLike`：活动点赞，约束 `unique_together = [['activity', 'member']]`
- `ClubDiscussion`：社团内部讨论，按 `-create_time` 排序，支持软删除字段 `is_deleted`

## 5. 表单与输入验证

### 5.1 `ClubModelForm`

- 管理社团信息
- 校验社长选择，可将成员对象转换为姓名保存到 `Club.president`
- 编辑时仅展示本社团成员作为社长候选
- 新增时仅展示未加入社团成员作为候选

### 5.2 `RoleModelForm`

- 管理角色名称与等级
- 仅管理员可操作

### 5.3 `MemberModelForm`

- 管理成员核心信息与社团/部门/角色关联
- 添加时：除 `club、role、avatar` 外字段必填
- 编辑时：禁用 `member_id` 字段，避免学号修改
- `save` 方法包含社长角色变更同步逻辑：
  - 将新增社长设置到社团表
  - 将同一社团原社长降级为普通成员
  - 若社长变更为非社长，清除原社团的 `Club.president`

### 5.4 管理员表单验证

- `MyadminForm`：账号唯一性验证、密码 MD5 加密、确认密码一致
- `MyadminFormEdit`：编辑账号时排除当前记录进行唯一性判断
- `MyadminFormReset`：密码重置时确认密码一致、MD5 加密

## 6. URL 路由与页面访问

### 6.1 登录与首页

- `/login/`：统一登录入口
- `/logout/`：退出登录
- `/`：仪表盘首页

### 6.2 管理员账户管理

- `/myadmin/list`
- `/myadmin/add`
- `/myadmin/<nid>/edit/`
- `/myadmin/delete/`
- `/myadmin/<nid>/reset/`

### 6.3 社团管理

- `/club/list`
- `/club/add`
- `/club/<nid>/edit/`
- `/club/delete/`
- `/club/transfer/`

### 6.4 角色管理

- `/role/list`
- `/role/add`
- `/role/<nid>/edit/`
- `/role/delete/`

### 6.5 成员管理

- `/member/list`
- `/member/add`
- `/member/<nid>/edit/`
- `/member/delete/`
- `/member/<nid>/reset/`

### 6.6 活动管理

- `/activity/list`
- `/activity/add`
- `/activity/<nid>/edit/`
- `/activity/<nid>/detail/`
- `/activity/<nid>/mark-attended/`
- `/activity/delete/`
- `/activity/get-members/`：AJAX 获取指定社团成员列表

### 6.7 活动报名管理

- `/registration/list`
- `/registration/add`
- `/registration/<nid>/edit/`
- `/registration/delete/`

### 6.8 招新管理

- `/recruitment/list`
- `/recruitment/add`
- `/recruitment/<nid>/edit/`
- `/recruitment/delete/`
- `/recruitment/<nid>/apply/`
- `/recruitment/applications/`
- `/recruitment/applications/add/`
- `/recruitment/applications/<nid>/edit/`
- `/recruitment/applications/delete/`
- `/recruitment/applications/<nid>/approve/`
- `/recruitment/applications/<nid>/reject/`

### 6.9 公告管理

- `/announcement/list`
- `/announcement/add`
- `/announcement/<nid>/edit/`
- `/announcement/<nid>/delete/`
- `/announcement/<nid>/detail/`

### 6.10 个人会员端

- `/member/profile/`
- `/member/change-password/`
- `/member/leave-club/`
- `/member/discussion/`
- `/member/discussion/<discussion_id>/delete/`
- `/member/my-registrations/`
- `/member/activities/`
- `/member/activities/<activity_id>/register/`
- `/member/activity/<activity_id>/`

### 6.11 个人主页与关注

- `/profile/<member_id>/`
- `/profile/<member_id>/follow/`

## 7. 功能流程与业务逻辑

### 7.1 登录流程

- 管理员登录：在 `MyAdmin` 表中按 `account` 和 MD5 密码匹配
- 成员登录：在 `Member` 表中按 `member_id` 和 MD5 密码匹配
- 成功登录后写入 `req.session['info']`，决定后续权限与视图

### 7.2 仪表盘展示逻辑

- 管理员：可查看全平台统计数据，也可按 `club_id` 查询某个社团数据
- 社长：仅显示本社团汇总数据
- 普通成员：显示个人报名数量与可报名活动数量

### 7.3 社团与社长关系

- 社团表 `Club.president` 存储社长姓名
- 社团编辑时可选择社长成员，保存后自动同步成员角色
- 社长转让页面允许将当前社长角色转给本社团其他成员
- 删除社团时，会将该社团成员的 `club` 和 `role` 清空

### 7.4 成员管理与角色同步

- 新增成员时，若未指定密码则默认 `123456`（MD5）
- 编辑成员时，若更改社团或角色，`MemberModelForm.save` 会同步原社长及主社团信息
- 成员删除时会删除该学号的所有招新申请记录，避免后续冲突
- 社长仅可操作本社团成员，管理员可操作全部成员

### 7.5 活动报名与数量统计

- 成员报名活动后，`ActivityRegistration` 记录创建
- 活动 `current_participants` 自动更新，统计状态为 `已报名` 或 `已参加`
- 组织者创建活动时自动报名并标记 `已参加`
- 活动详情展示报名记录、评论、照片、点赞数量
- 管理员/社长可批量将报名成员标记为 `已参加`

### 7.6 招新报名审核

- 招新批次由管理员或社长创建
- 成员通过 `/recruitment/<nid>/apply/` 提交报名申请
- 管理员/社长在 `application_list` 页面审核通过或拒绝
- 审核通过后，可转正式成员并分配所属社团、部门、角色
- 系统避免同一学号在同一批次重复报名

### 7.7 公告发布与显示规则

- 管理员可发布全局公告或指定社团公告
- 社长可发布本社团公告
- 普通成员只能查看已发布状态的全局公告与本社团公告
- `Announcement` 支持置顶和草稿状态

### 7.8 个人中心与社交功能

- 成员个人中心允许修改个人信息与密码
- 成员可查看自己的报名记录和可报名活动列表
- 成员可退出社团，但社长须先转让社长身份
- 个人主页展示个人资料、参与活动、关注/粉丝、共同活动
- 关注功能支持关注/取消关注操作
- 社团讨论模块支持成员发言与删除讨论

## 8. 辅助功能与实现细节

### 8.1 Excel 导出

- `app01.srcs.utils.export_excel.export_to_excel(rows, headers, filename, sheet_name)`
- 支持日期与时间格式化、列宽设置、表头加粗
- 被多个视图调用：社团、部门、成员、活动、报名、招新批次、招新报名

### 8.2 分页组件

- `app01.utils.page_nav.PageNav`
- 支持分页参数、页码导航、查询字符串保留
- 返回 `page_queryset` 与 `page_nav_string`

### 8.3 MD5 密码加密

- `app01.utils.md5.get_md5(password)`
- 所有密码字段存储的都是 MD5 摘要值

### 8.4 AJAX 功能

- `/activity/get-members/`：根据 `club_id` 返回成员下拉选项，主要用于活动组织者选择
- `/profile/<member_id>/follow/`：成员关注/取消关注接口，返回 JSON 结果

### 8.5 已废弃模块

- `app01.srcs.views/order.py`：订单功能已移除，返回 404
- `app01.srcs.views/task.py`：任务功能已移除或废弃
- `app01.srcs.views.user.py`：旧用户模块已弃用，当前由 `Member` 模型完全替代

## 9. 系统运行与部署

### 9.1 环境依赖

- Python 3.x
- Django 4.x
- MySQL 数据库（或 SQLite）
- `openpyxl` 用于 Excel 导出

### 9.2 初始化步骤

1. 安装依赖：`pip install -r requirement.txt`
2. 配置数据库：编辑 `Learntest/settings.py`
3. 生成迁移并迁移：
   - `python manage.py makemigrations`
   - `python manage.py migrate`
4. 创建管理员账号：
   ```python
   from app01.utils.md5 import get_md5
   from app01.models import MyAdmin
   MyAdmin.objects.create(id='admin', user_name='admin', account='admin', password=get_md5('12345678'))
   ```
5. 启动服务：`python manage.py runserver`

### 9.3 默认访问

- 登录页：`http://127.0.0.1:8000/login/`
- 首页：`http://127.0.0.1:8000/`

## 10. 扩展建议

- 实现基于角色的细粒度权限管理
- 增加日志审计与操作记录
- 添加消息通知与邮件提醒
- 完善活动社交：评论点赞排序、图片评论、评论回复
- 引入前后端分离：Django REST API + Vue/React 前端
- 增强移动端适配性
- 支持社团成员权限分组与多部门成员角色

---

> 本文档基于当前系统代码与功能模块生成，适合用于毕业设计撰写与后续扩展开发参考。